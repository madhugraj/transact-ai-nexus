import { Copyright } from "lucide-react";
const AppFooter = () => {
  const currentYear = new Date().getFullYear();
  return <footer className="border-t py-4 mt-auto">
      
    </footer>;
};
export default AppFooter;